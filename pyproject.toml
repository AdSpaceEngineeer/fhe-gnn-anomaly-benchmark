[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fhe-gnn-anomaly-benchmark"
version = "0.1.0"
description = "Reference utilities and specification for an FHE-protected identifier GNN anomaly benchmark"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}
authors = [{name = "AdSpaceEngineeer"}]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Topic :: Security :: Cryptography",
]
dependencies = ["numpy>=2.0"]

[project.optional-dependencies]
yelpchi = ["scipy>=1.12"]
tenseal = ["tenseal==0.3.16"]

[project.scripts]
fhe-gnn-benchmark = "fhe_gnn_anomaly_benchmark.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
