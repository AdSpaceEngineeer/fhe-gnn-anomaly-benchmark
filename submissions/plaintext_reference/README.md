# Plaintext protocol dry run

This submission exercises every client/server stage and produces the same
logical scores as the plaintext reference model. It performs **no encryption**,
provides **zero security**, and must never appear in an FHE performance table.
Its only purpose is end-to-end harness testing before a real FHE backend is
integrated.
